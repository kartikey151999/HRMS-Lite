## Packages
@tanstack/react-table | Data tables for employee listing (optional but helpful if needed, we will stick to native mappings for a cleaner lightweight UI as requested)
date-fns | Formatting dates for the attendance API (YYYY-MM-DD)
lucide-react | Icons for the UI

## Notes
- Using native array mapping for tables to keep the application lightweight and responsive, matching the "HRMS Lite" philosophy.
- The API expects dates in `YYYY-MM-DD` string format.
- Assuming Shadcn UI components are available in `@/components/ui/`.
